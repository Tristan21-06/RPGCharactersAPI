meta {
  name: Update character
  type: http
  seq: 2
}

patch {
  url: {{url}}/personnage/update/{{name}}
  body: formUrlEncoded
  auth: inherit
}

body:form-urlencoded {
  name: Tutu
}

vars:pre-request {
  name: Tete
}
